# Python
__pycache__/
*.py[cod]

# Ambiente virtual
.venv/
venv/

# Saída solta
output.txt

# Capturas de trabalho do template_selector: descartáveis.
tests/capture/

# Dataset de treino: imagens pesam e não entram no git.
# Faça backup por rsync/cópia, não por commit.

/dataset/images
/dataset/samples.jsonl

# Layout por resolução: dataset/data/<LARGURAxALTURA>/{images,samples.jsonl}
/dataset/data/

# dataset/cache/ (features já extraídas) É commitado de propósito: é a
# memória acumulada do treino. Apagar o dataset bruto não pode apagar isso —
# ver IA/feature_cache.py e IA/train_ai.py (category_dataset).

# Modelo treinado: derivado do cache, regenerado a cada `train_ai.py` local.
/IA/*.joblib


/dataset_to_unify/images
/dataset_to_unify/samples.jsonl

# tests/images/ NÃO entra aqui: são os fixtures em que o
# tests/golden/expectations.json se baseia.
.aider*

/tools/scrcpy/